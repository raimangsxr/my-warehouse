# Quickstart: Navegación padre en lotes

```bash
cd frontend
npm test -- --watch=false --include='src/app/items/intake-batches.component.spec.ts' --include='src/app/items/item-intake-batch.component.spec.ts'
npm test -- --watch=false
npm run build
```

Comprobación manual: abrir lista general, lista desde caja y detalle directamente; activar cada retorno y revisar la cabecera en ancho móvil.
