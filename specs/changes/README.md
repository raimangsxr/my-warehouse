# Active changes

- **002-delete-warehouse** — Eliminación permanente de almacenes con limpieza total (`specs/changes/002-delete-warehouse/spec.md`)

Para abrir otro change, usa `/speckit-specify` y actualiza `specs/manifest.yml`.
